{
  "name": "podhound",
  "module": "src/main.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run src/main.ts",
    "start:dev": "bun --watch src/main.ts",
    "test": "bun test",
    "lint": "biome check src test",
    "lint:fix": "biome check --write --unsafe src test",
    "build": "bun build --compile --minify ./src/main.ts --outfile dist/podhound"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5",
    "@types/bun": "latest",
    "typescript": "^7.0.0"
  }
}
